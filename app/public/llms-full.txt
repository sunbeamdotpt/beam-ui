# Beam Design Language — Full API Reference

> A comprehensive React component library by Sunbeam Studios for building design system interfaces.

## Overview
- **Package:** `@sunbeam/beam-ui`
- **Registry:** https://src.sunbeam.pt/api/packages/studio/npm/
- **Source:** https://src.sunbeam.pt/studio/beam-ui
- **Docs:** https://design.sunbeam.pt
- **Stack:** React 19, Panda CSS, Ark UI v4, TypeScript

## Install
```bash
npm install @sunbeam/beam-ui --registry=https://src.sunbeam.pt/api/packages/studio/npm/
```

## Foundations
- **Colors:** Warm sunbeam palette — primary orange (#fa520f), sunshine scale (#ff8a00 to #ffe295), semantic tokens for light/dark mode
- **Typography:** Ysabeau Infant (body/heading), Monaspace Argon (monospace)
- **Spacing:** 4px base unit scale
- **Elevation:** Golden shadow system with hover lift effects
- **Accessibility:** Section 508 + WCAG 2.1 AA compliant, keyboard navigable, screen reader tested

## Components (86 total)

### Accordion

> Accordion component.

```tsx
import { Accordion } from "@sunbeam/beam-ui/components/ui/accordion"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `AccordionEntry[]` | Yes |  |
| multiple | `boolean` | No |  |
| defaultValue | `string[]` | No |  |
| className | `string` | No |  |

---

### ActivityHeatmap

> ActivityHeatmap component.

```tsx
import { ActivityHeatmap } from "@sunbeam/beam-ui/components/ui/activity-heatmap"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| data | `ActivityDay[]` | Yes |  |
| className | `string` | No |  |

---

### AssigneePicker

> AssigneePicker component.

```tsx
import { AssigneePicker } from "@sunbeam/beam-ui/components/ui/assignee-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `UserOption[]` | Yes |  |
| selected | `string[]` | Yes |  |
| onChange | `(selected: string[]) => void` | Yes |  |
| placeholder | `string` | No |  |
| className | `string` | No |  |

---

### Avatar

> Avatar component.

```tsx
import { Avatar } from "@sunbeam/beam-ui/components/ui/avatar"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| name | `string` | Yes |  |
| src | `string` | No |  |
| size | `"sm" | "md" | "lg"` | No |  |
| className | `string` | No |  |

---

### Badge

> Badge component.

```tsx
import { Badge } from "@sunbeam/beam-ui/components/ui/badge"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `React.ReactNode` | Yes |  |
| variant | `BadgeVariant` | No |  |
| className | `string` | No |  |

---

### BentoItem

> BentoItem component.

```tsx
import { BentoItem } from "@sunbeam/beam-ui/components/ui/bento-item"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| variant | `"large" | "horizontal" | "small"` | Yes |  |
| title | `string` | Yes |  |
| description | `string` | Yes |  |
| difficulty | `string` | Yes |  |
| category | `string` | Yes |  |
| imageAlt | `string` | No |  |
| href | `string` | No |  |

---

### BranchSelector

> BranchSelector component.

```tsx
import { BranchSelector } from "@sunbeam/beam-ui/components/ui/branch-selector"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| branches | `string[]` | Yes |  |
| tags | `string[]` | Yes |  |
| current | `string` | Yes |  |
| defaultBranch | `string` | No |  |
| onChange | `(ref: string) => void` | Yes |  |
| onCreateBranch | `(name: string) => void` | No |  |
| className | `string` | No |  |

---

### Button

> Button component.

```tsx
import { Button } from "@sunbeam/beam-ui/components/ui/button"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `ReactNode` | Yes |  |
| variant | `Variant` | No |  |
| href | `string` | No |  |
| className | `string` | No |  |
| onClick | `() => void` | No |  |
| type | `"button" | "submit" | "reset"` | No |  |
| disabled | `boolean` | No |  |

---

### Callout

> Callout component.

```tsx
import { Callout } from "@sunbeam/beam-ui/components/ui/callout"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `ReactNode` | Yes |  |
| variant | `CalloutVariant` | No |  |
| className | `string` | No |  |

---

### CapabilityCard

> CapabilityCard component.

```tsx
import { CapabilityCard } from "@sunbeam/beam-ui/components/ui/capability-card"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| icon | `string` | Yes |  |
| title | `string` | Yes |  |
| description | `string` | Yes |  |
| className | `string` | No |  |

---

### Card

> Card component.

```tsx
import { Card } from "@sunbeam/beam-ui/components/ui/card"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| icon | `string` | Yes |  |
| title | `string` | Yes |  |
| description | `string` | Yes |  |
| ctaLabel | `string` | Yes |  |
| ctaHref | `string` | Yes |  |
| className | `string` | No |  |

---

### Checkbox

> Checkbox component.

```tsx
import { Checkbox } from "@sunbeam/beam-ui/components/ui/checkbox"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| checked | `boolean` | Yes |  |
| onChange | `(checked: boolean) => void` | Yes |  |
| label | `string` | No |  |
| disabled | `boolean` | No |  |
| indeterminate | `boolean` | No |  |
| className | `string` | No |  |

---

### Clipboard

> Clipboard component.

```tsx
import { Clipboard } from "@sunbeam/beam-ui/components/ui/clipboard"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| children | `ReactNode` | No |  |
| timeout | `number` | No |  |

---

### CodeBlock

> Content keyed by toggle combo, e.g. "Non-streaming|V2|Synchronous". Falls back to "default". */
  variants?: Record<string, ReactNode>;
  /** Simple content (no variants) */
  content?: ReactNode;
}

interface ToggleGroup {
  options: string[];
  defaultValue?: string;
}

interface CodeBlockProps {
  tabs: CodeTab[];
  streamToggle?: ToggleGroup;
  versionToggle?: ToggleGroup;
  modeToggle?: ToggleGroup;
  className?: string;
}

/* ------------------------------------------------------------------ */
/* Small pill toggle                                                   */
/* ------------------------------------------------------------------ */
function PillToggle({
  options,
  value,
  onChange,
}: {
  options: string[];
  value: string;
  onChange: (v: string) => void;
}) {
  return (
    <div role="group" className={css({ display: "flex", backgroundColor: "sunbeam.black", borderRadius: "md", padding: "2px" })}>
      {options.map((opt) => (
        <button
          key={opt}
          onClick={() => onChange(opt)}
          aria-pressed={value === opt}
          className={cx(
            css({
              padding: "4px 12px",
              fontSize: "10px",
              fontWeight: "button",
              borderRadius: "md",
              textTransform: "uppercase",
              border: "none",
              cursor: "pointer",
              transition: "all 0.15s ease",
              fontFamily: "body",
            }),
            value === opt
              ? css({ backgroundColor: "code.activePill", color: "white" })
              : css({ backgroundColor: "transparent", color: "rgba(255,255,255,0.35)", _hover: { color: "rgba(255,255,255,0.7)" } })
          )}
        >
          {opt}
        </button>
      ))}
    </div>
  );
}

/* ------------------------------------------------------------------ */
/* CodeBlock                                                           */
/* ------------------------------------------------------------------

```tsx
import { CodeBlock } from "@sunbeam/beam-ui/components/ui/code-block"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| tabs | `CodeTab[]` | Yes |  |
| streamToggle | `ToggleGroup` | No |  |
| versionToggle | `ToggleGroup` | No |  |
| modeToggle | `ToggleGroup` | No |  |
| className | `string` | No |  |

**Also exports:** `syn`

---

### CodeEditor

> CodeEditor component.

```tsx
import { CodeEditor } from "@sunbeam/beam-ui/components/ui/code-editor"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| language | `string` | No |  |
| height | `string` | No |  |
| readOnly | `boolean` | No |  |
| showLineNumbers | `boolean` | No |  |
| softWrap | `boolean` | No |  |
| placeholder | `string` | No |  |
| className | `string` | No |  |

---

### ColorPicker

> ColorPicker component.

```tsx
import { ColorPicker } from "@sunbeam/beam-ui/components/ui/color-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| presets | `string[]` | No |  |
| label | `string` | No |  |
| className | `string` | No |  |

---

### Combobox

> Combobox component.

```tsx
import { Combobox } from "@sunbeam/beam-ui/components/ui/combobox"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `ComboboxOption[]` | Yes |  |
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| placeholder | `string` | No |  |
| disabled | `boolean` | No |  |
| className | `string` | No |  |

---

### CommentThread

> CommentThread component.

```tsx
import { CommentThread } from "@sunbeam/beam-ui/components/ui/comment-thread"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `(Comment | TimelineEvent)[]` | Yes |  |
| onReply | `(body: string) => void` | No |  |
| onEdit | `(id: string, body: string) => void` | No |  |
| onReaction | `(commentId: string, emoji: string) => void` | No |  |
| className | `string` | No |  |

---

### CommitGraph

> CommitGraph component.

```tsx
import { CommitGraph } from "@sunbeam/beam-ui/components/ui/commit-graph"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| commits | `CommitNode[]` | Yes |  |
| className | `string` | No |  |

---

### ContextMenu

> ContextMenu component.

```tsx
import { ContextMenu } from "@sunbeam/beam-ui/components/ui/context-menu"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `ContextMenuItem[]` | Yes |  |
| children | `ReactNode` | Yes |  |

---

### DatePicker

> DatePicker component.

```tsx
import { DatePicker } from "@sunbeam/beam-ui/components/ui/date-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | No |  |
| onChange | `(value: string) => void` | No |  |
| label | `string` | No |  |
| placeholder | `string` | No |  |
| disabled | `boolean` | No |  |
| className | `string` | No |  |

---

### DiagramRenderer

> DiagramRenderer component.

```tsx
import { DiagramRenderer } from "@sunbeam/beam-ui/components/ui/diagram-renderer"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| code | `string` | Yes |  |
| className | `string` | No |  |

---

### Dialog

> Dialog component.

```tsx
import { Dialog } from "@sunbeam/beam-ui/components/ui/dialog"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| open | `boolean` | Yes |  |
| onClose | `() => void` | Yes |  |
| title | `string` | Yes |  |
| children | `ReactNode` | Yes |  |
| actions | `ReactNode` | No |  |

---

### DropdownMenu

> DropdownMenu component.

```tsx
import { DropdownMenu } from "@sunbeam/beam-ui/components/ui/dropdown-menu"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `DropdownMenuItem[]` | No | Flat list of items, or grouped items |
| groups | `DropdownMenuGroup[]` | No |  |
| children | `ReactNode` | Yes |  |

---

### Editable

> Editable component.

```tsx
import { Editable } from "@sunbeam/beam-ui/components/ui/editable"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| placeholder | `string` | No |  |
| className | `string` | No |  |

---

### EmptyState

> EmptyState component.

```tsx
import { EmptyState } from "@sunbeam/beam-ui/components/ui/empty-state"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| icon | `string` | No |  |
| title | `string` | Yes |  |
| description | `string` | No |  |
| action | `ReactNode` | No |  |

---

### FeatureTile

> FeatureTile component.

```tsx
import { FeatureTile } from "@sunbeam/beam-ui/components/ui/feature-tile"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| name | `string` | Yes |  |
| endpoint | `string` | Yes |  |
| icon | `string` | Yes |  |

---

### FileList

> FileList component.

```tsx
import { FileList } from "@sunbeam/beam-ui/components/ui/file-list"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `FileItem[]` | Yes |  |
| selected | `Set<string>` | Yes |  |
| onSelect | `(selected: Set<string>) => void` | Yes |  |
| onOpen | `(item: FileItem) => void` | No |  |
| layout | `"list" | "grid"` | No |  |
| className | `string` | No |  |

---

### FileUpload

> FileUpload component.

```tsx
import { FileUpload } from "@sunbeam/beam-ui/components/ui/file-upload"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| onFiles | `(files: File[]) => void` | Yes |  |
| accept | `string` | No |  |
| multiple | `boolean` | No |  |
| disabled | `boolean` | No |  |
| className | `string` | No |  |

---

### HoverCard

> HoverCard component.

```tsx
import { HoverCard } from "@sunbeam/beam-ui/components/ui/hover-card"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| trigger | `ReactNode` | Yes |  |
| children | `ReactNode` | Yes |  |
| className | `string` | No |  |

---

### Icon

> Icon component.

```tsx
import { Icon } from "@sunbeam/beam-ui/components/ui/icon"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| name | `string` | Yes |  |
| size | `number | string` | No |  |
| filled | `boolean` | No |  |
| className | `string` | No |  |
| label | `string` | No | When provided, the icon is treated as meaningful: role="img" + aria-label. Otherwise aria-hidden="true". |

---

### KanbanCardDetail

> KanbanCardDetail component.

```tsx
import { KanbanCardDetail } from "@sunbeam/beam-ui/components/ui/kanban-card-detail"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| card | `KanbanCardData` | Yes |  |
| open | `boolean` | Yes |  |
| onClose | `() => void` | Yes |  |
| onSave | `(card: KanbanCardData) => void` | No |  |
| onDelete | `(id: string) => void` | No |  |
| readOnly | `boolean` | No |  |
| className | `string` | No |  |

---

### KanbanCardView

> Standalone card display — usable outside the board (e.g., in lists, detail panels).

```tsx
import { KanbanCardView } from "@sunbeam/beam-ui/components/ui/kanban-board"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| columns | `KanbanColumn[]` | Yes |  |
| onChange | `(columns: KanbanColumn[]) => void` | Yes |  |
| onAddCard | `(columnId: string) => void` | No |  |
| className | `string` | No |  |

**Also exports:** `KanbanBoard`

---

### Kbd

> Kbd component.

```tsx
import { Kbd } from "@sunbeam/beam-ui/components/ui/kbd"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `string` | Yes |  |
| platform | `"mac" | "windows" | "linux"` | No | Override platform detection. Defaults to auto-detect. |
| className | `string` | No |  |

---

### LabelPicker

> LabelPicker component.

```tsx
import { LabelPicker } from "@sunbeam/beam-ui/components/ui/label-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `LabelOption[]` | Yes |  |
| selected | `string[]` | Yes |  |
| onChange | `(selected: string[]) => void` | Yes |  |
| placeholder | `string` | No |  |
| className | `string` | No |  |

---

### LineChart

> LineChart component.

```tsx
import { LineChart } from "@sunbeam/beam-ui/components/ui/charts"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| data | `ChartDataPoint[]` | Yes |  |
| lines | `{ key: string` | Yes |  |
| color | `string` | No |  |

**Also exports:** `BarChart`, `PieChart`, `AreaChart`

---

### List

> List component.

```tsx
import { List } from "@sunbeam/beam-ui/components/ui/list"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `ListItem[]` | Yes |  |
| ordered | `boolean` | No |  |
| variant | `Variant` | No |  |
| className | `string` | No |  |

---

### LoginForm

> LoginForm component.

```tsx
import { LoginForm } from "@sunbeam/beam-ui/components/ui/auth-form"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| onSubmit | `(username: string, password: string, remember: boolean) => void` | Yes |  |
| oauthProviders | `OAuthProvider[]` | No |  |
| error | `string` | No |  |
| loading | `boolean` | No |  |

**Also exports:** `SignUpForm`, `ForgotPasswordForm`, `TwoFactorForm`

---

### MarkdownEditor

> MarkdownEditor component.

```tsx
import { MarkdownEditor } from "@sunbeam/beam-ui/components/ui/markdown-editor"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| placeholder | `string` | No |  |
| minHeight | `string` | No |  |
| className | `string` | No |  |

---

### MarkdownRenderer

> MarkdownRenderer component.

```tsx
import { MarkdownRenderer } from "@sunbeam/beam-ui/components/ui/markdown-renderer"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| content | `string` | Yes |  |
| className | `string` | No |  |

---

### MathRenderer

> MathRenderer component.

```tsx
import { MathRenderer } from "@sunbeam/beam-ui/components/ui/math-renderer"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| math | `string` | Yes |  |
| display | `boolean` | No |  |
| className | `string` | No |  |

---

### MilestonePicker

> MilestonePicker component.

```tsx
import { MilestonePicker } from "@sunbeam/beam-ui/components/ui/milestone-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `MilestoneOption[]` | Yes |  |
| selected | `string | null` | Yes |  |
| onChange | `(selected: string | null) => void` | Yes |  |
| placeholder | `string` | No |  |
| className | `string` | No |  |

---

### ModelRow

> ModelRow component.

```tsx
import { ModelRow } from "@sunbeam/beam-ui/components/ui/model-row"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| name | `string` | Yes |  |
| icon | `string` | Yes |  |
| tier | `string` | Yes |  |
| version | `string` | Yes |  |
| description | `string` | Yes |  |
| href | `string` | No |  |

---

### NotificationItem

> Material Symbol icon name, or a ReactNode for custom icons */
  icon?: string;
  title: string;
  /** Grouping key — notifications are grouped by this value (e.g., repo name, app name, channel) */
  group?: string;
  /** Secondary text — timestamp, sender, channel, etc. */
  subtitle?: string;
  /** Human-readable timestamp */
  timestamp?: string;
  read: boolean;
  /** Optional action URL */
  href?: string;
  /** Any additional metadata the consumer wants to attach */
  meta?: Record<string, unknown>;
}

interface NotificationCenterProps {
  notifications: Notification[];
  onMarkRead: (id: string) => void;
  onMarkAllRead: () => void;
  onClickNotification?: (notification: Notification) => void;
  /** Header title. Defaults to "Notifications" */
  title?: string;
  /** Trigger icon. Defaults to "notifications" */
  triggerIcon?: string;
  /** Make groups collapsible with unread count badges. Defaults to false. */
  collapsibleGroups?: boolean;
  className?: string;
}

/** Built-in icon suggestions for common notification types */
export const notificationIcons = {
  // Code forge
  issue: "bug_report",
  pr: "merge",
  release: "new_releases",
  mention: "alternate_email",
  review: "rate_review",
  commit: "commit",
  // Communication
  email: "mail",
  chat: "chat",
  dm: "forum",
  // Calendar
  event: "event",
  reminder: "alarm",
  // Storage
  upload: "cloud_upload",
  share: "share",
  // System
  security: "security",
  update: "system_update",
  warning: "warning",
  info: "info",
} as const;

/** Standalone notification row — usable outside the NotificationCenter dropdown.

```tsx
import { NotificationItem } from "@sunbeam/beam-ui/components/ui/notification-center"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| notifications | `Notification[]` | Yes |  |
| onMarkRead | `(id: string) => void` | Yes |  |
| onMarkAllRead | `() => void` | Yes |  |
| onClickNotification | `(notification: Notification) => void` | No |  |
| title | `string` | No | Header title. Defaults to "Notifications" |
| triggerIcon | `string` | No | Trigger icon. Defaults to "notifications" |
| collapsibleGroups | `boolean` | No | Make groups collapsible with unread count badges. Defaults to false. |
| className | `string` | No |  |

**Also exports:** `NotificationCenter`, `notificationIcons`

---

### NumberInput

> NumberInput component.

```tsx
import { NumberInput } from "@sunbeam/beam-ui/components/ui/number-input"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `number` | Yes |  |
| onChange | `(value: number) => void` | Yes |  |
| min | `number` | No |  |
| max | `number` | No |  |
| step | `number` | No |  |
| label | `string` | No |  |

---

### Pagination

> Pagination component.

```tsx
import { Pagination } from "@sunbeam/beam-ui/components/ui/pagination"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| currentPage | `number` | Yes |  |
| totalPages | `number` | Yes |  |
| onPageChange | `(page: number) => void` | Yes |  |
| pageSize | `number` | No |  |
| onPageSizeChange | `(size: number) => void` | No |  |
| className | `string` | No |  |

---

### parseDiff

> parseDiff component.

```tsx
import { parseDiff } from "@sunbeam/beam-ui/components/ui/diff-viewer"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| hunks | `DiffHunk[]` | Yes |  |
| oldFileName | `string` | No |  |
| newFileName | `string` | No |  |
| mode | `"unified" | "split"` | No |  |
| className | `string` | No |  |

**Also exports:** `DiffViewer`

---

### PinInput

> PinInput component.

```tsx
import { PinInput } from "@sunbeam/beam-ui/components/ui/pin-input"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| length | `number` | No |  |
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| mask | `boolean` | No |  |
| label | `string` | No |  |

---

### Popover

> Popover component.

```tsx
import { Popover } from "@sunbeam/beam-ui/components/ui/popover"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| trigger | `ReactNode` | Yes |  |
| children | `ReactNode` | Yes |  |
| title | `string` | No |  |
| className | `string` | No |  |

---

### ProgressBar

> ProgressBar component.

```tsx
import { ProgressBar } from "@sunbeam/beam-ui/components/ui/progress-bar"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `number` | Yes |  |
| variant | `ProgressVariant` | No |  |
| showLabel | `boolean` | No |  |
| size | `"sm" | "md"` | No |  |
| className | `string` | No |  |

---

### RadioGroup

> RadioGroup component.

```tsx
import { RadioGroup } from "@sunbeam/beam-ui/components/ui/radio-group"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `RadioOption[]` | Yes |  |
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| label | `string` | No |  |

---

### ReactionPicker

> ReactionPicker component.

```tsx
import { ReactionPicker } from "@sunbeam/beam-ui/components/ui/reaction-picker"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| reactions | `Reaction[]` | Yes |  |
| onToggle | `(emoji: string) => void` | Yes |  |
| onAdd | `(emoji: string) => void` | Yes |  |
| className | `string` | No |  |

---

### ScrollArea

> ScrollArea component.

```tsx
import { ScrollArea } from "@sunbeam/beam-ui/components/ui/scroll-area"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `ReactNode` | Yes |  |
| maxHeight | `string` | No |  |
| scrollbar | `"visible" | "hover" | "auto"` | No | "visible" always shows themed scrollbar, "hover" hides until hover, "auto" uses browser default with theme colors |
| direction | `"vertical" | "horizontal" | "both"` | No | Scroll direction |
| className | `string` | No |  |

---

### SearchInput

> SearchInput component.

```tsx
import { SearchInput } from "@sunbeam/beam-ui/components/ui/search-input"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| className | `string` | No |  |

---

### Select

> Select component.

```tsx
import { Select } from "@sunbeam/beam-ui/components/ui/select"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| options | `SelectOption[]` | Yes |  |
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| placeholder | `string` | No |  |
| disabled | `boolean` | No |  |
| className | `string` | No |  |

---

### Skeleton

> Skeleton component.

```tsx
import { Skeleton } from "@sunbeam/beam-ui/components/ui/skeleton"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| width | `string` | No |  |
| height | `string` | No |  |
| variant | `"text" | "circle" | "rect"` | No |  |
| count | `number` | No |  |
| className | `string` | No |  |

---

### Slider

> Slider component.

```tsx
import { Slider } from "@sunbeam/beam-ui/components/ui/slider"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `number` | Yes |  |
| onChange | `(value: number) => void` | Yes |  |
| min | `number` | No |  |
| max | `number` | No |  |
| step | `number` | No |  |
| label | `string` | No |  |

---

### Spinner

> Spinner component.

```tsx
import { Spinner } from "@sunbeam/beam-ui/components/ui/spinner"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| size | `"sm" | "md" | "lg"` | No | Size of the spinner |
| label | `string` | No | Optional label shown below the spinner |
| accent | `boolean` | No | Use brand color instead of muted |
| color | `string` | No | Custom color for the spinner arc (overrides accent) |
| className | `string` | No |  |

---

### Splitter

> Splitter component.

```tsx
import { Splitter } from "@sunbeam/beam-ui/components/ui/splitter"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| children | `[ReactNode, ReactNode]` | Yes |  |
| direction | `"horizontal" | "vertical"` | No |  |
| defaultSize | `number` | No |  |

---

### StatBar

> StatBar component.

```tsx
import { StatBar } from "@sunbeam/beam-ui/components/ui/stat-bar"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| stats | `ModelStats` | Yes |  |

---

### Steps

> Steps component.

```tsx
import { Steps } from "@sunbeam/beam-ui/components/ui/steps"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| steps | `StepItem[]` | Yes |  |
| currentStep | `number` | Yes |  |
| onChange | `(step: number) => void` | No |  |

---

### Switch

> Switch component.

```tsx
import { Switch } from "@sunbeam/beam-ui/components/ui/switch"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| checked | `boolean` | Yes |  |
| onChange | `(checked: boolean) => void` | Yes |  |
| label | `string` | No |  |
| disabled | `boolean` | No |  |
| className | `string` | No |  |

---

### SyntaxHighlighter

> SyntaxHighlighter component.

```tsx
import { SyntaxHighlighter } from "@sunbeam/beam-ui/components/ui/syntax-highlighter"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| code | `string` | Yes |  |
| language | `string` | Yes |  |
| theme | `"light" | "dark"` | No |  |
| showLineNumbers | `boolean` | No |  |
| highlightLines | `number[]` | No |  |
| className | `string` | No |  |

---

### Table

> Table component.

```tsx
import { Table } from "@sunbeam/beam-ui/components/ui/table"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| columns | `Column[]` | Yes |  |
| rows | `Array<Record<string, any>>` | Yes |  |
| onSort | `(key: string, dir: "asc" | "desc") => void` | No |  |
| selectable | `boolean` | No |  |
| onSelect | `(selectedKeys: string[]) => void` | No |  |
| rowKey | `string` | No |  |
| className | `string` | No |  |
| caption | `string` | No | Accessible caption for the table (visually hidden by default) |

---

### Tabs

> Tabs component.

```tsx
import { Tabs } from "@sunbeam/beam-ui/components/ui/tabs"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `TabItem[]` | Yes |  |
| activeValue | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| variant | `"default" | "dark"` | No |  |

---

### TagsInput

> TagsInput component.

```tsx
import { TagsInput } from "@sunbeam/beam-ui/components/ui/tags-input"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string[]` | Yes |  |
| onChange | `(value: string[]) => void` | Yes |  |
| placeholder | `string` | No |  |
| max | `number` | No |  |
| label | `string` | No |  |

---

### TextInput

> TextInput component.

```tsx
import { TextInput } from "@sunbeam/beam-ui/components/ui/text-input"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| placeholder | `string` | No |  |
| label | `string` | No |  |
| error | `string` | No |  |
| disabled | `boolean` | No |  |
| type | `"text" | "password" | "email" | "number"` | No |  |
| className | `string` | No |  |

---

### ThemeToggle

> ThemeToggle component.

```tsx
import { ThemeToggle } from "@sunbeam/beam-ui/components/ui/theme-toggle"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| variant | `"icon" | "switch" | "pill"` | No | "icon" shows sun/moon, "switch" shows a labeled toggle, "pill" shows a segmented control |
| className | `string` | No |  |

---

### Toast

> Toast component.

```tsx
import { Toast } from "@sunbeam/beam-ui/components/ui/toast"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| message | `string` | Yes |  |
| variant | `ToastVariant` | No |  |
| visible | `boolean` | Yes |  |
| onDismiss | `() => void` | No |  |
| onShow | `() => void` | No |  |

---

### Toggle

> Toggle component.

```tsx
import { Toggle } from "@sunbeam/beam-ui/components/ui/toggle"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| pressed | `boolean` | Yes |  |
| onChange | `(pressed: boolean) => void` | Yes |  |
| children | `ReactNode` | Yes |  |
| className | `string` | No |  |

---

### ToggleGroup

> ToggleGroup component.

```tsx
import { ToggleGroup } from "@sunbeam/beam-ui/components/ui/toggle-group"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `ToggleGroupOption[]` | Yes |  |
| value | `string` | Yes |  |
| onChange | `(value: string) => void` | Yes |  |
| className | `string` | No |  |

---

### Tooltip

> Tooltip component.

```tsx
import { Tooltip } from "@sunbeam/beam-ui/components/ui/tooltip"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| content | `string` | Yes |  |
| children | `ReactNode` | Yes |  |
| position | `"top" | "bottom" | "left" | "right"` | No |  |

---

### TopicCard

> TopicCard component.

```tsx
import { TopicCard } from "@sunbeam/beam-ui/components/ui/topic-card"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| title | `string` | Yes |  |
| icon | `string` | Yes |  |
| description | `string` | Yes |  |
| href | `string` | No |  |

---

### TransferList

> TransferList component.

```tsx
import { TransferList } from "@sunbeam/beam-ui/components/ui/transfer-list"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| available | `TransferItem[]` | Yes |  |
| selected | `TransferItem[]` | Yes |  |
| onChange | `(available: TransferItem[], selected: TransferItem[]) => void` | Yes |  |
| availableTitle | `string` | No |  |
| selectedTitle | `string` | No |  |
| className | `string` | No |  |

---

### TreeView

> TreeView component.

```tsx
import { TreeView } from "@sunbeam/beam-ui/components/ui/tree-view"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| nodes | `TreeNode[]` | Yes |  |
| activeId | `string` | No |  |
| className | `string` | No |  |

---

### Wizard

> If true, the Next button is disabled until the consumer sets it to false */
  isValid?: boolean;
}

export interface WizardProps {
  steps: WizardStep[];
  onComplete: () => void;
  onCancel?: () => void;
  /** Called when step changes — receives the new step index */
  onStepChange?: (step: number) => void;
  /** Labels for the buttons */
  nextLabel?: string;
  backLabel?: string;
  completeLabel?: string;
  cancelLabel?: string;
  className?: string;
}

export interface WizardModalProps extends WizardProps {
  open: boolean;
  onClose: () => void;
  title?: string;
}

/* ------------------------------------------------------------------ */
/* Wizard                                                              */
/* ------------------------------------------------------------------

```tsx
import { Wizard } from "@sunbeam/beam-ui/components/ui/wizard"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| steps | `WizardStep[]` | Yes |  |
| onComplete | `() => void` | Yes |  |
| onCancel | `() => void` | No |  |
| onStepChange | `(step: number) => void` | No | Called when step changes — receives the new step index |
| nextLabel | `string` | No | Labels for the buttons |
| backLabel | `string` | No |  |
| completeLabel | `string` | No |  |
| cancelLabel | `string` | No |  |
| className | `string` | No |  |

**Also exports:** `WizardModal`

---

### WorkItemList

> WorkItemList component.

```tsx
import { WorkItemList } from "@sunbeam/beam-ui/components/ui/work-item-list"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `WorkItemRow[]` | Yes |  |
| selected | `Set<string>` | No |  |
| onSelect | `(selected: Set<string>) => void` | No |  |
| selectable | `boolean` | No |  |
| onLoadMore | `() => void` | No |  |
| className | `string` | No |  |

---

### Breadcrumbs

> Breadcrumbs component.

```tsx
import { Breadcrumbs } from "@sunbeam/beam-ui/components/shell/breadcrumbs"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `Array<{ label: string` | Yes |  |

---

### Footer

> Footer component.

```tsx
import { Footer } from "@sunbeam/beam-ui/components/shell/footer"
```

_No documented props._

---

### Header

> Header component.

```tsx
import { Header } from "@sunbeam/beam-ui/components/shell/header"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| showThemeToggle | `boolean` | No | Show a theme toggle button (right-aligned). Defaults to true. |

---

### RightRail

> RightRail component.

```tsx
import { RightRail } from "@sunbeam/beam-ui/components/shell/right-rail"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| items | `Array<{ label: string` | Yes |  |

---

### Shell

> Shell component.

```tsx
import { Shell } from "@sunbeam/beam-ui/components/shell/shell"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| showThemeToggle | `boolean` | No | Show the theme toggle in the header. Defaults to true. |
| header | `ReactNode` | No | Replace the default Header with a custom element. |
| footer | `ReactNode` | No | Replace the default Footer with a custom element. |
| children | `ReactNode` | No | Content to render. If omitted, renders <Outlet /> for React Router. |
| className | `string` | No |  |

---

### Sidebar

> Sidebar component.

```tsx
import { Sidebar } from "@sunbeam/beam-ui/components/shell/sidebar"
```

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| sections | `NavSection[]` | Yes |  |

---

### ApiLayout

> ApiLayout component.

```tsx
import { ApiLayout } from "@sunbeam/beam-ui/components/layouts/api-layout"
```

_No documented props._

**Also exports:** `apiLeftPanel`, `apiRightPanel`

---

### FullwidthLayout

> FullwidthLayout component.

```tsx
import { FullwidthLayout } from "@sunbeam/beam-ui/components/layouts/fullwidth-layout"
```

_No documented props._

---

### useDocsContext

> useDocsContext component.

```tsx
import { useDocsContext } from "@sunbeam/beam-ui/components/layouts/docs-layout"
```

_No documented props._

**Also exports:** `DocsLayout`
